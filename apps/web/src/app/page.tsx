import { Button, Input } from "@handson/ui/heroui";

export default function Home() {
  return (
    <div>
      <main>
        <Button>Hello</Button>
        <Input label="Email" placeholder="Enter your email" />
      </main>
    </div>
  );
}
